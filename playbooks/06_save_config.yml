---
- name: Save Cisco IOS XE running configuration
  hosts: ios_routers
  gather_facts: false
  connection: ansible.netcommon.network_cli

  tasks:
    - name: Save running-config to startup-config
      cisco.ios.ios_config:
        save_when: always
